#ifndef DIE_YORDLE
#define DIE_YORDLE

#include <string>

class maths
{
	public:

    	int maths::power(long a, long long b); // +
    	int maths::pgcd(int a, int b); // +
    	//void maths::factoriser(int n); // -

	private:

};

class fonctions
{
	public:

		//void fonctions::~reception(); // -
		void fonctions::public_key(); // +
		void fonctions::prived_key(); // +
		void fonctions::cryptage(unsigned char message);
		void fonctions::decryptage();
		
	private:

		int p, q, e, n, phin;
		unsigned char document[128];
		unsigned char crypted_message[128];
};

#endif

class noyade
{
	public:

		void noyade::deconvolution();
		void noyade::analyse();
		void noyade::integration();

	private:
		
		void test();
		double *~nirlya();
		int *verysta();
		double serya();

		double omega, ceta, summ;
}

class explode_sim
{
	public:
		explode_sim();
		~explode_sim(int* thats_what_she_said);
	
};
